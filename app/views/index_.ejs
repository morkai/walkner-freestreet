<!DOCTYPE html>
<html>
  <head>
    <meta charset=utf-8>
    <title>Tester opraw LED</title>
    <link rel=stylesheet href="min.css">
  </head>
  <body>
    <div id=overlay>
      <img src="loading.gif" width=16 height=16 alt="Proszę czekać...">
    </div>
    <div id=hd>
      <ul class="buttons left"></ul>
      <ul class="buttons right"></ul>
      <h1>WhatWhat?!</h1>
    </div>
    <div id=bd></div>
    <script src="/lib/vendor.js"></script>
    <script src="/socket.io/socket.io.js"></script>
    <script src="/min.js"></script>
    <script>
    $(function()
    {
      boot({
        testerModels: {{- JSON.stringify(testerModels) }},
        testers: {{- JSON.stringify(testers) }},
        programs: {{- JSON.stringify(programs) }},
        users: {{- JSON.stringify(users) }},
        user: {{- JSON.stringify(user ? user : null) }}
      });
    });
    </script>
    {{- partial('template', templates) }}
  </body>
</html>
